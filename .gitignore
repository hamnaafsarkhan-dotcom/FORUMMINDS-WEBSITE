# ForumMinds — static site. There is no build step, so there is almost
# nothing to ignore. Only OS/editor droppings.

# Windows / macOS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Registration backend — delegate PII, never committed. See register-handler.php.
registrations.csv
